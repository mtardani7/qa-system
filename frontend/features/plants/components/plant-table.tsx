"use client";

import { CheckCircle2, CircleOff, ChevronLeft, ChevronRight, Edit2, Search, Trash2 } from "lucide-react";
import { useEffect, useState } from "react";
import { Button } from "@/components/ui/button";
import { Skeleton } from "@/components/ui/skeleton";
import { useDeletePlant, usePlants } from "../hooks";
import type { Plant } from "../types";

type Props = { onEdit?: (plant: Plant) => void };
type LoadingPlaceholderProps = { className?: string };
const Loader2 = (props: LoadingPlaceholderProps) => { void props; return <Skeleton className="h-12 w-full" />; };
export function PlantTable({ onEdit }: Props) {
  const [searchInput, setSearchInput] = useState("");
  const [search, setSearch] = useState("");
  const [page, setPage] = useState(1);
  const deleteMutation = useDeletePlant();
  useEffect(() => { const timer = window.setTimeout(() => { setSearch(searchInput); setPage(1); }, 300); return () => window.clearTimeout(timer); }, [searchInput]);
  const { data, isLoading, isError } = usePlants({ search, page, per_page: 20 });
  const remove = (plant: Plant) => { if (window.confirm(`Delete ${plant.name}? This action cannot be undone.`)) deleteMutation.mutate(plant.id); };
  return <section className="rounded-2xl border border-slate-200 bg-white shadow-sm dark:border-slate-800 dark:bg-slate-900"><div className="flex flex-col gap-4 border-b border-slate-200 p-5 sm:flex-row sm:items-center sm:justify-between dark:border-slate-800"><div><h2 className="text-base font-semibold text-slate-950 dark:text-white">Plant registry</h2><p className="mt-1 text-sm text-slate-500">{data?.total ?? 0} production sites</p></div><label className="flex h-10 items-center gap-2 rounded-lg border border-slate-200 px-3 text-sm dark:border-slate-700"><Search className="size-4 text-slate-400" /><input className="w-full bg-transparent outline-none sm:w-56" placeholder="Search code or name..." value={searchInput} onChange={(event) => setSearchInput(event.target.value)} /></label></div>{isLoading ? <div className="flex h-48 items-center justify-center"><Loader2 className="size-5 animate-spin text-emerald-600" /></div> : isError ? <p className="p-6 text-sm text-rose-600">Unable to load plants. Check the API connection.</p> : data?.data.length === 0 ? <div className="p-10 text-center text-sm text-slate-500">No plants match your search.</div> : <><div className="overflow-x-auto"><table className="w-full min-w-[680px] text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500 dark:bg-slate-950/40"><tr><th className="px-5 py-3">Code</th><th className="px-5 py-3">Name</th><th className="px-5 py-3">Description</th><th className="px-5 py-3">Status</th><th className="px-5 py-3 text-right">Actions</th></tr></thead><tbody className="divide-y divide-slate-100 dark:divide-slate-800">{data?.data.map((plant) => <tr key={plant.id} className="hover:bg-slate-50 dark:hover:bg-slate-800/50"><td className="px-5 py-4 font-mono text-xs font-semibold text-emerald-700">{plant.code}</td><td className="px-5 py-4 font-medium text-slate-900 dark:text-white">{plant.name}</td><td className="px-5 py-4 text-slate-500">{plant.description ?? "—"}</td><td className="px-5 py-4">{plant.is_active ? <span className="inline-flex items-center gap-1.5 text-emerald-600"><CheckCircle2 className="size-4" /> Active</span> : <span className="inline-flex items-center gap-1.5 text-slate-400"><CircleOff className="size-4" /> Inactive</span>}</td><td className="px-5 py-4"><div className="flex justify-end gap-1"><Button variant="ghost" size="icon-sm" onClick={() => onEdit?.(plant)} aria-label={`Edit ${plant.name}`}><Edit2 className="size-4" /></Button><Button variant="ghost" size="icon-sm" className="text-rose-600 hover:text-rose-700" onClick={() => remove(plant)} disabled={deleteMutation.isPending} aria-label={`Delete ${plant.name}`}><Trash2 className="size-4" /></Button></div></td></tr>)}</tbody></table></div><div className="flex items-center justify-between border-t border-slate-200 px-5 py-3 dark:border-slate-800"><p className="text-xs text-slate-500">Page {data?.current_page ?? 1} of {data?.last_page ?? 1}</p><div className="flex gap-2"><Button variant="outline" size="sm" disabled={!data || data.current_page <= 1} onClick={() => setPage((current) => current - 1)}><ChevronLeft className="size-4" /> Previous</Button><Button variant="outline" size="sm" disabled={!data || data.current_page >= data.last_page} onClick={() => setPage((current) => current + 1)}>Next <ChevronRight className="size-4" /></Button></div></div></>}</section>;
}
