import type { LucideIcon } from "lucide-react";
export type DocSection = { heading: string; paragraphs?: string[]; bullets?: string[] };
export function DocArticle({ eyebrow, title, description, icon: Icon, sections }: { eyebrow: string; title: string; description: string; icon: LucideIcon; sections: DocSection[] }) { return <article className="max-w-4xl"><div className="flex size-12 items-center justify-center rounded-2xl bg-emerald-100 text-emerald-700 dark:bg-emerald-950/60 dark:text-emerald-300"><Icon className="size-6" /></div><p className="mt-7 text-sm font-semibold uppercase tracking-wider text-emerald-600">{eyebrow}</p><h1 className="mt-3 text-4xl font-semibold tracking-tight sm:text-5xl">{title}</h1><p className="mt-5 max-w-3xl text-lg leading-8 text-muted-foreground">{description}</p><div className="mt-12 space-y-10">{sections.map((section) => <section key={section.heading} className="scroll-mt-24"><h2 className="text-2xl font-semibold tracking-tight">{section.heading}</h2>{section.paragraphs?.map((paragraph) => <p key={paragraph} className="mt-4 leading-7 text-muted-foreground">{paragraph}</p>)}{section.bullets && <ul className="mt-4 space-y-3 text-muted-foreground">{section.bullets.map((bullet) => <li key={bullet} className="flex gap-3 leading-7"><span className="mt-2 size-1.5 shrink-0 rounded-full bg-emerald-500" />{bullet}</li>)}</ul>}</section>)}</div></article>; }
