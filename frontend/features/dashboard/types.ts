export type DashboardQuery = { date: string; period?: "daily" | "weekly" | "monthly" | "yearly"; plant_id?: number; line_id?: number; shift_id?: number; machine_id?: number; product_id?: number; checker_id?: number };
export type DashboardSummary = { production_pcs: number; defect_qty: number; defect_rate: number; yield: number };
export type DashboardDefect = { id: number; report_id?: number; code: string; name: string; category: string; quantity: number; cumulative_percentage?: number };
export type DashboardRanking = { id: number; report_id?: number; code: string; name: string; value: number; output_pcs?: number };
export type DashboardTrend = { date: string; report_id?: number; production_pcs: number; defect_qty: number; defect_rate: number; yield: number };
export type DashboardKpis = { production: number; output_pcs: number; defects: number; defect_rate: number; yield: number };
export type DashboardKpi = { target_yield: number; actual_yield: number; yield_indicator: "green" | "yellow" | "red"; target_defect_rate: number; actual_defect_rate: number; defect_rate_indicator: "green" | "yellow" | "red" };
export type DashboardData = { date: string; summary: DashboardSummary; today: DashboardKpis; monthly: DashboardKpis; kpi: DashboardKpi; top_defects: DashboardDefect[]; pareto: DashboardDefect[]; top_machines: DashboardRanking[]; top_products: DashboardRanking[]; monthly_trend: DashboardTrend[]; production_trend: DashboardTrend[]; defect_trend: DashboardTrend[]; yield_trend: DashboardTrend[]; comparisons: { shifts: DashboardRanking[]; machines: DashboardRanking[]; plants: DashboardRanking[] } };
