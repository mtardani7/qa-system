import type { ReactNode } from "react";

type Row = { code: string; name: string; value: number; secondary?: string };
type Props = { title: string; rows: Row[]; valueLabel: string; icon?: ReactNode };
export function DashboardRanking({ title, rows, valueLabel, icon }: Props) { const max = Math.max(...rows.map((row) => row.value), 1); return <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><div className="flex items-center gap-2"><span className="text-emerald-600">{icon}</span><h2 className="font-semibold text-slate-950 dark:text-white">{title}</h2></div><div className="mt-5 space-y-4">{rows.length === 0 ? <p className="text-sm text-slate-500">No data for the selected filters.</p> : rows.map((row, index) => <div key={`${row.code}-${row.name}`}><div className="flex items-center justify-between text-sm"><span className="min-w-0 truncate text-slate-700 dark:text-slate-300"><span className="mr-2 text-xs text-slate-400">{index + 1}.</span>{row.name}</span><span className="ml-3 whitespace-nowrap font-semibold text-slate-900 dark:text-white">{row.value.toLocaleString()} {valueLabel}</span></div><div className="mt-2 h-2 rounded-full bg-slate-100 dark:bg-slate-800"><div className="h-2 rounded-full bg-emerald-500" style={{ width: `${Math.max((row.value / max) * 100, 3)}%` }} /></div>{row.secondary && <p className="mt-1 text-xs text-slate-500">{row.secondary}</p>}</div>)}</div></section>; }
