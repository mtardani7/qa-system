import type { DashboardTrend } from "../types";
export function MonthlyTrend({ rows }: { rows: DashboardTrend[] }) { const max = Math.max(...rows.map((row) => row.production_pcs), 1); return <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h2 className="font-semibold text-slate-950 dark:text-white">Monthly trend</h2><div className="mt-5 space-y-3">{rows.length === 0 ? <p className="text-sm text-slate-500">No production recorded this month.</p> : rows.map((row) => <div key={row.date} className="grid grid-cols-[4rem_1fr_5rem] items-center gap-3 text-xs"><span className="text-slate-500">{row.date.slice(5)}</span><div className="h-5 rounded bg-slate-100 dark:bg-slate-800"><div className="h-5 rounded bg-emerald-500" style={{ width: `${Math.max((row.production_pcs / max) * 100, 2)}%` }} /></div><span className="text-right font-medium text-slate-700 dark:text-slate-300">{row.production_pcs.toLocaleString()}</span></div>)}</div><div className="mt-4 flex gap-4 text-xs text-slate-500"><span><i className="mr-1 inline-block size-2 rounded-full bg-emerald-500" />Output PCS</span><span>Defects tracked in daily report</span></div></section>; }
