import type { DashboardDefect } from "../types";
export function ParetoChart({ rows }: { rows: DashboardDefect[] }) { return <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h2 className="font-semibold text-slate-950 dark:text-white">Defect Pareto</h2><p className="mt-1 text-sm text-slate-500">Cumulative contribution of the top defect causes.</p><div className="mt-5 space-y-3">{rows.length === 0 ? <p className="text-sm text-slate-500">No defects recorded for the selected filters.</p> : rows.map((row) => <div key={row.id} className="grid grid-cols-[minmax(0,1fr)_3rem_4rem] items-center gap-3 text-sm"><span className="truncate text-slate-700 dark:text-slate-300">{row.name}</span><span className="text-right font-semibold text-slate-900 dark:text-white">{row.quantity}</span><span className="text-right text-xs text-emerald-600">{row.cumulative_percentage}%</span></div>)}</div></section>; }
