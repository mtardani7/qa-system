import type { MetadataRoute } from "next";
export default function manifest(): MetadataRoute.Manifest { const basePath = process.env.NEXT_PUBLIC_BASE_PATH ?? ""; return { name: "QA Management System", short_name: "QA System", description: "Manufacturing quality management workspace", start_url: `${basePath}/dashboard`, display: "standalone", background_color: "#f8fafc", theme_color: "#059669", icons: [{ src: `${basePath}/qa-logo.png`, sizes: "any", type: "image/png" }] }; }
