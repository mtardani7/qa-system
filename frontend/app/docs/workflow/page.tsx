import { Workflow } from "lucide-react"; import { DocArticle } from "@/features/docs/components/doc-article";
export default function WorkflowPage() { return <DocArticle eyebrow="Workflow" title="A controlled path from output to insight" icon={Workflow} description="Quality data moves through a clear lifecycle so every decision can be trusted." sections={[{ heading: "Daily Report process", bullets: ["Operator or QA staff selects plant, date, shift, line, machine, and product.", "The system loads product quantity and calculates Output PCS.", "Defects are selected from master data and their category is resolved automatically.", "The report is saved as Draft and can be submitted for review."] }, { heading: "Approval process", bullets: ["QA Supervisor reviews the submitted report.", "The report can be approved, rejected for correction, or locked.", "Locked reports are read-only for operational users.", "Audit history records create, edit, review, approval, rejection, and lock actions."] }, { heading: "Dashboard and export", paragraphs: ["Dashboards aggregate production and child defect quantities without multiplying totals. Exports are generated through queues and can be filtered by date, plant, shift, machine, product, and checker."] }]}/>; }
