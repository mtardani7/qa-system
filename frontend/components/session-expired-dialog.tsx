"use client";
import { useEffect, useState } from "react";
export function SessionExpiredDialog() { const [open, setOpen] = useState(false); useEffect(() => { const handler = () => setOpen(true); window.addEventListener("qms:session-expired", handler); return () => window.removeEventListener("qms:session-expired", handler); }, []); if (!open) return null; return <div className="fixed inset-0 z-[100] flex items-center justify-center bg-slate-950/50 p-6" role="dialog" aria-modal="true" aria-labelledby="session-expired-title"><div className="w-full max-w-sm rounded-2xl bg-white p-6 shadow-xl dark:bg-slate-900"><h2 id="session-expired-title" className="text-lg font-semibold">Session expired</h2><p className="mt-2 text-sm text-muted-foreground">Your session has expired. Redirecting you to sign in...</p></div></div>; }
